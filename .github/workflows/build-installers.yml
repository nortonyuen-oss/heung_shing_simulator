name: Build Installers

on:
  workflow_dispatch:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-15
            command: npm run dist:mac -- --publish never
            artifact: mac-installers
          - os: windows-latest
            command: npm run dist:win -- --publish never
            artifact: windows-installers

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build installers
        shell: bash
        run: ${{ matrix.command }}

      - name: Upload installer artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: |
            release/*.dmg
            release/*.zip
            release/*.exe
            release/*.blockmap
            release/latest*.yml
          if-no-files-found: error

  release:
    name: Publish GitHub Release
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest

    steps:
      - name: Download installer artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release assets
        shell: bash
        run: |
          set -euo pipefail
          version="${GITHUB_REF_NAME#v}"
          mkdir -p release-assets
          mac_dmg="$(find artifacts/mac-installers -maxdepth 1 -type f -name '*.dmg' | head -n 1)"
          win_setup="$(find artifacts/windows-installers -maxdepth 1 -type f -name '*Setup*.exe' | head -n 1)"
          win_portable="$(find artifacts/windows-installers -maxdepth 1 -type f -name '*.exe' ! -name '*Setup*' | head -n 1)"
          test -n "$mac_dmg"
          test -n "$win_setup"
          test -n "$win_portable"
          cp "$mac_dmg" "release-assets/The.City.of.Heung.Shing-${version}.dmg"
          cp "$win_setup" "release-assets/The.City.of.Heung.Shing.Setup.${version}.exe"
          cp "$win_portable" "release-assets/The.City.of.Heung.Shing.${version}.exe"
          ls -lh release-assets

      - name: Create release and upload installers
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          version="${GITHUB_REF_NAME#v}"
          gh release create "$GITHUB_REF_NAME" \
            --title "The City of Heung Shing ${GITHUB_REF_NAME}" \
            --notes "Desktop installers for The City of Heung Shing ${GITHUB_REF_NAME}." \
            release-assets/*
